<!-- Conventional Commit title, e.g. feat(spec): add ask_foo -->

## What & why

<!-- one paragraph -->

## Checklist

- [ ] `pnpm -r build` and `pnpm -r test` pass locally
- [ ] `@elicitkit/conformance` not weakened (fixtures added if capability changed)
- [ ] SPEC.md / ROADMAP.md updated if behavior or the catalog changed
- [ ] New type? spec + core + all 4 renderers + conformance fixtures all landed
- [ ] Commits signed off (DCO: `git commit -s`) — contribution is Apache-2.0
- [ ] Reserved spec hooks (`agentGuess`/`confidence`/`memoryKey`) untouched
